function solve(currentString, startIndex, stopIndex) {
    let result = String;
    result = currentString.substring(startIndex, startIndex + stopIndex)
    console.log(result)
    
}

// solve('ASentence', 1, 8)