function solve(firstChar, secondChar, thirdChar){
    let finalArray = []
    finalArray.push(firstChar)
    finalArray.push(secondChar)
    finalArray.push(thirdChar)
    finalString = finalArray.join("")
    console.log(finalString)
}

// solve("a", "b", "c")